{
  "name": "sentinel-logistics",
  "version": "1.0.0",
  "description": "Sentinel Logistics - Unified Vercel Deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
